/**
 * CONTOUR PROCESSING FOR CV APPLICATION. Pulls image from ImageProcessor, finds contours, and calculates where to aim the shooter.
 * written by: Brach Knutson, FRC3695
 */

import java.lang.*;

public class ContourProcessor implements Runnable {
    int three = 0;
    public void run() {

        //temp, need to have something run so I dont get the big error
        three = 2+1;
    }
}